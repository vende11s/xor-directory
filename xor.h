#include<iostream>

void xor_file(std::string path, std::string key);
void refresh(int a, int x, int y, std::string message);
bool gotoxy(int x, int y);

static int increase_y(0); //if count of lines is not default
